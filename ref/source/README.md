Upstream source: https://github.com/I7-Examples/Glass/blob/main/Glass.inform/Source/story.ni
Local committed copy: `glass.ni` (renamed from upstream `story.ni` for clarity).
Extraction reads it directly (`just extract` → `data/graph.json`); the snapshot
URL above only restores it if missing.
